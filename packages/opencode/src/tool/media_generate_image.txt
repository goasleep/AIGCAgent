Generate an image with the configured media image model (gpt-image-2 or agnes-image-*), independent from the conversation/reasoning model. Never pass the conversation model here. For image editing, provide a project-relative `image` path and optionally a transparent `mask` path; editing currently requires the OpenAI-compatible provider. This is a paid API call — the user is asked for confirmation with a cost estimate before it runs. The result is saved into the project media library and attached to the conversation as an image. Retries are NOT idempotent: re-calling with the same prompt generates and charges again.
